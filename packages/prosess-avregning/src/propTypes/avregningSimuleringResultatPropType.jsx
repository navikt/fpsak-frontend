import PropTypes from 'prop-types';

const avregningSimuleringResultatPropType = PropTypes.shape({
  simuleringResultat: PropTypes.shape(),
  simuleringResultatUtenInntrekk: PropTypes.shape(),
});

export default avregningSimuleringResultatPropType;
