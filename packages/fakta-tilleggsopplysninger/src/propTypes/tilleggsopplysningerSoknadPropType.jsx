import PropTypes from 'prop-types';

const tilleggsopplysningerSoknadPropType = PropTypes.shape({
  tilleggsopplysninger: PropTypes.string.isRequired,
});

export default tilleggsopplysningerSoknadPropType;
