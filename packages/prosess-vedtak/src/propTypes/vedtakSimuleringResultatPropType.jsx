import PropTypes from 'prop-types';

const vedtakSimuleringResultatPropType = PropTypes.shape({
  simuleringResultat: PropTypes.shape(),
  simuleringResultatUtenInntrekk: PropTypes.shape(),
});

export default vedtakSimuleringResultatPropType;
